FROM ubuntu:latest

RUN apt-get update -qq && apt-get install -qq \
                gcc libssl-dev libelf-dev make bc build-essential git ccache

ENV CC="ccache gcc"
ENV CCACHE_DIR=/mnt/ccache
ENV CCACHE_NOCOMPRESS=1
