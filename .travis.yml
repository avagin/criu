language: c
sudo: required
dist: trusty
services:
  - docker
env:
  - TR_ARCH=local       GCOV=1
script:
  - sudo make -C scripts/travis $TR_ARCH
after_success:
  - make -C scripts/travis after_success
